export namespace AuthTheme {
    namespace _default {
        namespace colors {
            let brand: string;
            let brandAccent: string;
            let brandButtonText: string;
            let defaultButtonBackground: string;
            let defaultButtonBackgroundHover: string;
            let defaultButtonBorder: string;
            let defaultButtonText: string;
            let dividerBackground: string;
            let inputBackground: string;
            let inputBorder: string;
            let inputBorderHover: string;
            let inputBorderFocus: string;
            let inputText: string;
            let inputLabelText: string;
            let inputPlaceholder: string;
            let messageText: string;
            let messageTextDanger: string;
            let anchorTextColor: string;
            let anchorTextHoverColor: string;
        }
        namespace space {
            let spaceSmall: string;
            let spaceMedium: string;
            let spaceLarge: string;
            let labelBottomMargin: string;
            let anchorBottomMargin: string;
            let emailInputSpacing: string;
            let socialAuthSpacing: string;
            let buttonPadding: string;
            let inputPadding: string;
        }
        namespace fontSizes {
            let baseBodySize: string;
            let baseInputSize: string;
            let baseLabelSize: string;
            let baseButtonSize: string;
        }
        namespace fonts {
            let bodyFontFamily: string;
            let buttonFontFamily: string;
            let inputFontFamily: string;
            let labelFontFamily: string;
        }
        namespace borderWidths {
            let buttonBorderWidth: string;
            let inputBorderWidth: string;
        }
        namespace radii {
            let borderRadiusButton: string;
            let buttonBorderRadius: string;
            let inputBorderRadius: string;
        }
    }
    export { _default as default };
}
